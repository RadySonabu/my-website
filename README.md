# my-website

> TODO: one-line project purpose, once `blueprint/project-plan.md` is filled in.

## Commands

- Dev server: `npm run dev` (http://localhost:3000)
- Build: `npm run build`
- Production server: `npm run start`
- Lint: `npm run lint`

See `AGENTS.md` for the full workflow and conventions.
